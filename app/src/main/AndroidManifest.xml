<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission
        android:name="android.permission.CHANGE_CONFIGURATION"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ApiDemo"
        tools:targetApi="s">
        <activity
            android:name="com.example.apidemo.BeaconActivity"
            android:exported="true"
            android:label="VPOS">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.apidemo.BleConnectActivity"
            android:exported="false"
            android:label="BLE Connect" />
        <activity
            android:name="com.example.apidemo.PaymentActivity"
            android:exported="false"
            android:label="Payment" />
        <activity
            android:name="com.example.apidemo.SuccessActivity"
            android:exported="false"
            android:label="Success" />
        <activity
            android:name="com.example.apidemo.SettingsActivity"
            android:exported="false"
            android:label="Settings" />

        <receiver
            android:name="com.example.apidemo.receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>